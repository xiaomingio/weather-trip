---
/**
 * 文件说明: 将语言根路径重定向到带查看模式的默认工具页路径。
 * 对应文档: docs/data-flow.md
 */
return Astro.redirect(
  `/${Astro.params.locale === 'en' ? 'en' : 'zh'}/${Astro.url.searchParams.get('mode') === 'daily' ? 'daily' : 'travel'}`
);
---
