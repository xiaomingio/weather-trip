# 文件说明: Web app 本机环境变量模板。复制为同目录 .env.development 后供本地 dev/start 使用。
# 生产公开配置（域名、Umami、R2 公开读入口）真源是 src/domain/site-config.ts；当前静态站不使用 .env.production。

# ===== Web Dev Server =====
WEB_HOST=127.0.0.1
WEB_PORT=3000
